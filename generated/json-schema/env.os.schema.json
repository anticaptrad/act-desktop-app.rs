{
  "$id": "https://github.com/flags-2-env/flags-2-env-cli/generated/json-schema/act-desktop-app/env.os.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Resolved environment map for DesktopEnv after flags-2-env overlay (flags > env_shell > env_file unless reordered). Values are still strings, as the OS stores them. Validate this object at runtime with `check_os_env` or `f2e check-contract`; do not hand-edit generated sources.",
  "properties": {
    "ACT_DESKTOP_APP_BIND": {
      "description": "Runtime environment key ACT_DESKTOP_APP_BIND.",
      "examples": [
        "127.0.0.1:8080",
        "0.0.0.0:8080"
      ],
      "minLength": 1,
      "type": "string",
      "x-env-key": "ACT_DESKTOP_APP_BIND",
      "x-flag-type": "string"
    }
  },
  "title": "DesktopEnv resolved environment",
  "type": "object",
  "x-flags-2-env": {
    "generator": "flags-2-env",
    "service": "act-desktop-app",
    "typeName": "DesktopEnv"
  }
}
